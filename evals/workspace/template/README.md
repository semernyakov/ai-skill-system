# Workspace Template

Use this structure for iteration-based evaluation runs.

- `iteration-*/runs/` stores per-test run artifacts.
- `iteration-*/grading.json` stores per-test grading output.
- `iteration-*/benchmark.json` stores aggregate metrics.
- `comparison.json` at workspace root can store iteration diffs.
